{
  "name": "hacking-mini-games",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A series of hacking mini-games with a 3D dotted-halftone point-cloud visual identity.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/three": "^0.170.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
